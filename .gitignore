# 빌드 산출물
_site
.dev-site

# NPM
node_modules
# package-lock.json 은 커밋한다 — CI 의 `npm ci` 가 이 파일을 요구하고,
# 없으면 빌드마다 의존성 버전이 달라진다.

# IDE
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# OS
.DS_Store
